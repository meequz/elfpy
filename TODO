- Make these two lines one:
```
test called test.A()
test called test.A(140379755009584).__init__()
```

- Add tests
- Check when two calls in one func
- Check when code is in __init__.py file
- Check for static methods
- Recognize when single function performs return (check do we really need it)
- Add visualization
- NovelFormatter may have much more beautiful output
- Exclude standart lib?
- Django support
- Any other loop-based aps support
- Ability to ban some calls with settings
- Ability to stop after specified calls number
